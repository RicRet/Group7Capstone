import { Stylesheet } from "react_native"; // need to download

const Login: { // needs to figure out stack
    // vars go in here

    const handleLogin = async () => {
        // needs to use API calls
    }

    return (
        
    )
};

const styles = StyleSheet.create({
    text: {
        // template
    }
});

export default Login;